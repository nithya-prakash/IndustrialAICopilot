server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: unknown paths (client-side routes) fall back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
